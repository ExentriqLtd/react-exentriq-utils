/** @format */

import { TRoom } from "../declarations/rooms";

export const utilityOrderRoomsByDate = (rooms: TRoom[]): TRoom[] =>
  rooms.slice().sort((a: TRoom, b: TRoom): number => b.lm.$date - a.lm.$date);
